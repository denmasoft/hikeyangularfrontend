import {IDictionaryDefinition} from '@i18n/models/dictionary-definition';

/**
 * Spanish dictionary of the Flight Module.
 */
export const PageCoverEnDictionary = <IDictionaryDefinition>{
    locale: 'en',
    terms: {
        'pageCover.pageTitle': 'Flight Search Result',
        'pageCover.home': 'Home'
    }
};
